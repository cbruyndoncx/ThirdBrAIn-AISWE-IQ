The {{action}} Git action failed with the following error:
```
{{error}}
```

Investigate the cause of this failure and resolve it:
- If you can fix it yourself (e.g., repository state, local changes, configuration, or a code issue), use your tools to fix it.
- If you cannot fix it automatically (e.g., it requires credentials, remote access, or a decision only the user can make), do not guess. Instead, propose a clear solution with concrete, actionable steps the user should take.
