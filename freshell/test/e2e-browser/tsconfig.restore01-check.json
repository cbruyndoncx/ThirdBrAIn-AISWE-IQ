{
  "//": "RESTORE-01 item-scoped typecheck gate (same pattern as tsconfig.browser01-check.json / tsconfig.term04-check.json). The repo-owned typecheck configs deliberately exclude test/, so this gives the recovery auto-decline helper + its adopting specs an executable gate: run `npx tsc -p test/e2e-browser/tsconfig.restore01-check.json`, then require zero error lines attributable to the files RESTORE-01 created or edited. Errors in pre-existing dependency files reproduce identically on the base commit and are out of scope.",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    "types": ["node"]
  },
  "include": [
    "helpers/recovery-offer.ts",
    "helpers/recovery-offer.test.ts",
    "helpers/fixtures.ts",
    "specs/multi-client.spec.ts",
    "specs/tabs-client-retire.spec.ts",
    "specs/project-colors-matrix.spec.ts",
    "specs/sidebar-registry-sync-rust.spec.ts",
    "specs/restore-contract-wall-rust.spec.ts"
  ]
}
