# Adopt `@anthropic-ai/claude-agent-sdk` 0.2.71 → `^0.3.195` Implementation Plan

> Executed 2026-08-19 against bf832289738c2b4a43baeebb80d70e7c5654a3b5; the source tree supersedes all code and patch listings in this document. Only the `## User Request` block, Goal, Architecture, Global Constraints, and task intent remain authoritative.

> **For agentic workers:** REQUIRED: Use the usual-subagents (subagent-driven-development / executing-plans) and the-usual TDD (test-driven-development) subskills to implement this plan task-by-task and subtask-by-subtask. Steps use checkbox (`- [ ]`) syntax for tracking.

**User Request:**

> "Now inventory what changes the fork made vs base v0.7.0." (delivered; user review confirmed the highest-value item is the Claude Agent SDK 0.3.195 upgrade)
>
> "Use the-usual to adopt the SDK upgrade."

## 1. Context lineage

- Prior completed stages: (a) fork-vs-base v0.7.0 inventory (user-ratified verdict: adopt the SDK upgrade; out of scope: attachments, live tool results, history fixes, IPv6, image paste, slash-command advertising, Tailscale MTU); (b) workspace stage: base suite green gate passed 2026-08-18, worktree `.worktrees/sdk-0-3-upgrade` on branch `the-usual/sdk-0-3-upgrade`, base_ref `b4d1d814807ed419e94c8d89399d8946e74df7a6`, `npm ci` done; (c) Stage 1 exploration: reports `plan-*.md` in logs dir; (d) **Stage 2 load-bearing validation complete**: 10 claims, ledger + finder/strategist/validator reports in `/home/dan/code/freshell/.worktrees/.the-usual-logs/sdk-0-3-upgrade/` (`load-bearing-ledger.md`, `reports/load-bearing-*.md`). Verified highlights: full usage surface (14 `Options` fields, all re-exported types) compiles clean against BOTH published 0.3.195 and 0.3.235 type defs (VAL-1 tsc probe); lockfile dry-run exit 0 with churn confined to the SDK closure (VAL-2); 0.3.x message-union additions are all dispatch-tolerant and `result.subtype 'success'` survives (VAL-1); bundled linux-x64 CLI is a real version-stamped ELF with PATH/creds prerequisites satisfied (VAL-1). Falsified and corrected below: several execution anchors cited from the fork era (LB-6/7/8/9) and the contract-suite coverage label (LB-10).
- Goal: upgrade main's `@anthropic-ai/claude-agent-sdk` from `^0.2.40` (locked 0.2.71) to `^0.3.195` (floats to newest 0.3.x — today 0.3.235 per VAL-2) without breaking freshclaude/kilroy sessions, locking the defensive behaviors (clean env, `settingSources`, benign-shape tolerance) with characterization tests, surfacing the 0.3 `supportedModels()`-probed catalog (with aliases) in the freshclaude/kilroy settings selector, and bumping the default model only if live 0.3-catalog evidence invalidates `claude-opus-4-6`.
- Fork reference (palimpsest, not source of truth): `/tmp/opencode/fork-analysis/freshell` = fork tree, `/tmp/opencode/fork-analysis/base` = upstream v0.7.0 (`d21f167c`), `changes.md` = fork author's overview. Do NOT cherry-pick fork commits; re-derive against current main.
- This plan covers ONLY the SDK upgrade seam.

## 2. Goal

Deliver a worktree branch that installs and runs on `@anthropic-ai/claude-agent-sdk ^0.3.195`, proves freshclaude/kilroy session health against the real 0.3.x bundled CLI (probe + agent-api scripted smoke through the SdkBridge), locks defensive options behavior with regression tests, exposes probed claude model aliases in the settings selector, and (conditional on evidence) bumps the shipped default model — ready for user-approved PR creation.

## 3. Architecture

Main's `server/sdk-bridge.ts` is already 0.3-safe (VAL-1 verified type-compat): `createClaudeSdkCleanEnv` strips only `CLAUDECODE`/`ANTHROPIC_API_KEY`; `createClaudeSdkOptions` sets `settingSources: ['user','project','local']`; message routing is `private handleSdkMessage(sessionId, msg)` with switch arms for `system` / `assistant` / `result` / `stream_event` and a debug-log-and-ignore default arm; resume passes `input.resumeSessionId` via the top-level `resume` option (no legacy `sessionId` fallback exists). Both claude providers (freshclaude, kilroy) share one options surface via `server/fresh-agent/adapters/claude/adapter.ts` → `SdkBridge.createSession` → `createClaudeSdkOptions`; kilroy additionally shares the model-capability registry probe. `server/fresh-agent/model-capability-registry.ts` probes SDK `supportedModels()` through a 10s-abort harness with alias-aware `normalizeFreshAgentModelCapabilityCatalog` (`canon`/`aliases`), `readModelId` (raw `value`→normalized `id`), and typed errors on garbage — which `server/fresh-agent/model-capabilities-router.ts` (`getCapabilities`/`createFailure` chain, `GET /api/fresh-agent/model-capabilities/:sessionType`) converts to 200-fresh / typed-503; responses are `{ok:true,status:'fresh'|'cached',models:[…]}` / `ok:false` (validated shapes, LB-9). Normalized client capability entries are STRICT-schema objects `{id, displayName, provider, supportsEffort, supportedEffortLevels, supportsAdaptiveThinking}` (`FreshAgentModelCapabilitySchema`), NOT `{value, description}` (LB-8). Client: `src/lib/fresh-agent-model-capabilities.ts` fetches capabilities; `src/components/fresh-agent/FreshAgentSettingsButton.tsx` builds static-only `modelOptions` (L83) for the simple-list radio UI (radio `onChange` list, gate on `modelOptions.length > 0`), fetches capabilities only for freshopencode into `opencodeCapabilities` (L79/L137-144), identifies the claude family via `paneContent.provider === 'claude'` (L89 `keepsSimpleModelList`), and renders statics instantly (no loading state exists; the opencode list simply swaps rows when the probe resolves — LB-7).

## 4. Tech stack

Existing only: Node.js/Express + `ws` server (tsx watch), React 18 + Redux Toolkit + Vite client, Vitest (unit/jsdom + server NodeNext configs), Playwright e2e, `@anthropic-ai/claude-agent-sdk ^0.3.195`. Resolution facts (VAL-2, executed dry-run + control): installs `@anthropic-ai/claude-agent-sdk@0.3.235` (today), auto-adds `@anthropic-ai/sdk@0.117.1` (peer `>=0.93.0`), raises `@modelcontextprotocol/sdk` 1.28.0 → `1.30.0` single copy (declared `^1.27.1` ∩ peer `^1.29.0` = satisfied — no ERESOLVE), zod stays single `4.3.6`. Churn: 2 changed (agent-sdk, mcp-sdk), 14 added (8 `@anthropic-ai/claude-agent-sdk-<platform>` binaries + `@anthropic-ai/sdk` + transitives json-schema-to-ts/ts-algebra/standardwebhooks/@stablelib/base64/fast-sha256), 16 removed (old SDK's `@img/sharp-*` / libvips optionals). No new dev dependencies.

## 5. Global constraints

- **Worktree only**: all changes in `/home/dan/code/freshell/.worktrees/sdk-0-3-upgrade`, branch `the-usual/sdk-0-3-upgrade`. No mutation of `main` / the live 3001 Rust server. No PR without explicit user approval.
- **TDD**: every closing subtask ends with "new behavior first (functionality test), then structural regression test, then refactor". RED evidence = failing output pasted into the run ledger; GREEN = passing output; REFACTOR = real pass. Mechanical steps (lockfile churn, `npm install`) have no useful RED — record why per step.
- **Tests are behavior contracts**: no prose-matching/config-grep/faux tests. Mocks put VALID data through the real schema boundaries: capability fixtures MUST match the strict `FreshAgentModelCapabilitySchema` shape (LB-8) — the existing component-test fixture convention (`CATALOG_RESPONSE`) is the model. Each test must be observable in code reach, not call-signature-only assertions.
- **Typed-error routing precedent** (replaces earlier fictional citation per LB-6f): the capabilities path validates with typed errors (`invalidCapabilityPayload`) thrown by the registry and converted by the router's `createFailure` into typed 503s — the precedent behavior is real; the mechanism name in the explorer report was wrong and no new validation machinery is needed.
- **npm lock-diff safeguard**: after `npm install`, `git diff package-lock.json` must show churn ONLY inside the allowed closure (VAL-2 list: `@anthropic-ai/claude-agent-sdk` + 8 platform binaries, `@anthropic-ai/sdk` + its 5 transitives, `@modelcontextprotocol/sdk`; removals: old sharp/libvips optionals). Anything else = STOP, report.
- **freshopencode/freshcodex/codex terminal mode unchanged** — every task that broadens a claude-only slot keeps non-claude providers byte-identical.
- **Scratch servers on unique recorded ports** (3493 for the smoke), PID to `/tmp/freshell-3493.pid`, killed by that PID only. Copy `/home/dan/code/freshell/.env` into the worktree root for the smoke (gitignored; `PORT=3493` on the command line wins because dotenv does not override existing env — `server/index.ts` dotenv/config at top; verified LB-9).
- **Coordinator-respect**: broad runs via `npm test` / `npm run test:vitest -- ...` (`FRESHELL_VITEST_BACKEND=cloud` set); never raw `npx vitest`; never kill foreign coordinator holders.
- **Fork reference discipline**: fork code is a design witness only. Every task cites what it corresponds to and where we deliberately deviate (selector: we pre-compute merged options so the radio `onChange` round-trips the value it rendered).

## 6. File responsibility map

| File | Change |
|---|---|
| `package.json` | L93: `"@anthropic-ai/claude-agent-sdk": "^0.2.40"` → `"^0.3.195"` |
| `package-lock.json` | Regenerated (churn confined per §5 safeguard; expected set = VAL-2 receipt) |
| `test/unit/server/sdk-bridge.test.ts` | +1 describe: options-construction lock (`createClaudeSdkCleanEnv` strip-set + `createClaudeSdkOptions` `settingSources`/`abortController`); +1 describe: benign 0.3 control shapes driven through the REAL routing seam (existing `mockMessages` generator + session `subscribe`/message listeners, file's L27-59 convention) asserting no user/assistant/result broadcast; default-arm shapes additionally debug-log-only, `system`-subtype shapes silent (per handleSdkMessage routing) |
| `src/lib/fresh-agent-model-capabilities.ts` | +1 exported pure function `mergeClaudeSelectorOptions` (see Task 4 interface) |
| `src/components/fresh-agent/FreshAgentSettingsButton.tsx` | Rename `opencodeCapabilities` state slot → `probedCapabilities`; extend fetch effect to `provider === 'claude'` sessions; merge probed rows into `modelOptions` for claude providers |
| `test/unit/client/lib/fresh-agent-model-capabilities.test.ts` | +tests for the merge helper |
| `test/unit/client/components/fresh-agent/FreshAgentSettingsButton.test.tsx` | +render test: freshclaude popover fires fetch once and renders probed alias + full-ID rows; no duplicate ids; existing opencode cases unchanged |
| `shared/fresh-agent-models.ts` + `src/lib/fresh-agent-registry.ts` (freshclaude + kilroy `defaultModel` entries) | CONDITIONAL (Task 5): default bump only on live-probe evidence |
| Rollback | §8 below (this committed plan IS the durable rollback doc) |

## 7. Tasks

### Task 1: Dependency bump + baseline characterization (mechanical; no TDD RED)

**Files:** `package.json` (L93), `package-lock.json` (generated)

- [ ] **Step 1:** Edit `package.json` L93 → `"@anthropic-ai/claude-agent-sdk": "^0.3.195"`.
- [ ] **Step 2:** `npm install 2>&1 | tail -5` and verify the resolved set matches the executed dry-run receipt (VAL-2): `npm ls @anthropic-ai/claude-agent-sdk @anthropic-ai/sdk @modelcontextprotocol/sdk zod` → expect agent-sdk `0.3.235` (or whatever newest-0.3.x is now — record the actual), `@anthropic-ai/sdk@0.117.1`-line, mcp-sdk `1.30.0` single copy, zod `4.3.6` unchanged.
- [ ] **Step 3:** Lock-diff safeguard per §5: churn outside the VAL-2 closure list = STOP and report (the 16 sharp/libvips removals are EXPECTED — old closure's optionals, not collateral damage).
- [ ] **Step 4:** GREEN check `npm run typecheck; echo "typecheck_exit=$?"` — expect PASS (VAL-1 tsc probe already compiled the full usage surface clean against 0.3.195 AND 0.3.235; this confirms in-tree). FAIL ⇒ Appendix A.
- [ ] **Step 5:** Focused suites (explicit `--config` — without it the coordinator prepends `run --config` and the bare `run` token degrades into a filename filter; see Task 3 Step 1 note): `npm run test:vitest -- run test/unit/server/sdk-bridge.test.ts test/unit/server/fresh-agent/ --config config/vitest/vitest.server.config.ts`. Expect PASS.
- [ ] **Step 6:** Impacted-server subtree: `npm run test:vitest -- run test/unit/server/ --config config/vitest/vitest.server.config.ts` — expect PASS.
- [ ] **Step 7:** Commit: `deps: bump @anthropic-ai/claude-agent-sdk to ^0.3.195`.

### Task 2: Runtime evidence against the real 0.3-bundled CLI (no production changes; must succeed unconditionally)

A real `claude` CLI (2.1.235 on PATH) + OAuth creds exist (VAL-1 stat-verified). The 0.3.x bundled linux-x64 CLI is a real version-stamped ELF (VAL-1). The real-CLI contract suite (`test/integration/real/coding-cli-session-contract.test.ts`) exercises the PATH `claude` binary's external behavior — NOT the SDK's bundled CLI (LB-10 relabel); bundled-CLI runtime coverage comes from this task's probe + smoke, which is exactly why both are hard STOP gates. All receipts (exact outputs) appended to the run ledger.

**Files:** none modified (receipts to run ledger only)

- [ ] **Step 1 (live probe):**
  ```bash
  FRESHELL_RUN_LIVE_SUPPORTED_MODELS_PROBE=1 npx tsx test/unit/server/fresh-agent/model-capability-registry.supportedModels.probe.ts | tee /tmp/sdk03-probe.json
  ```
  (Probe module self-executes its `main()` when run directly; prints `{rawModels, normalizedModels, parsedModelIds}`; validates through `FreshAgentModelCapabilitiesSchema` itself.) PASS = exit 0, `rawModels` non-empty, normalize+parse succeed. Record whether `parsedModelIds` contains `claude-opus-4-6` and/or dated variants (Task 5 oracle). Timeout/error exit = typed probe failure → Task 5 skips the bump with receipt; continue task order.
- [ ] **Step 2 (real-CLI contract gate):**
  ```bash
  FRESHELL_RUN_REAL_PROVIDER_CONTRACTS=1 npm run test:vitest -- run test/integration/real/coding-cli-session-contract.test.ts --config config/vitest/vitest.server.config.ts
  ```
  PASS = green (PATH-claude external behavior unchanged post-upgrade). Failure = STOP.
- [ ] **Step 3 (agent-api smoke through the SdkBridge → bundled CLI):** fixtures and response shapes below verified in LB-9 (`x-auth-token` header; `agent:"claude"`→freshclaude mapping; create returns `{status:'ok',data:{tabId,paneId,sessionId,sessionRef}}`; send-keys blocks via `waitForFreshAgentIdle`, `timeout` in SECONDS, returns HTTP 200 with `status:'approx'` on deadline-miss; capture returns `text/plain` rendered transcript):
  ```bash
  cp /home/dan/code/freshell/.env /home/dan/code/freshell/.worktrees/sdk-0-3-upgrade/.env
  mkdir -p /tmp/opencode/sdk03-smoke-cwd          # REQUIRED: no cwd validation/mkdir server-side (LB-9a)
  cd /home/dan/code/freshell/.worktrees/sdk-0-3-upgrade
  PORT=3493 npm run dev:server > /tmp/freshell-3493.log 2>&1 & echo $! > /tmp/freshell-3493.pid
  for i in $(seq 1 60); do curl -sf http://127.0.0.1:3493/api/health > /dev/null && break; sleep 1; done
  curl -sf http://127.0.0.1:3493/api/health > /dev/null || { echo "scratch server failed to boot in 60s"; sed -n 1,80p /tmp/freshell-3493.log; kill "$(cat /tmp/freshell-3493.pid)" 2>/dev/null; rm -f /tmp/freshell-3493.pid; exit 1; }   # real cap + failure evidence + self-cleaning (unauthenticated endpoint)
  source .env   # AUTH_TOKEN (PORT in file is overridden by env above)
  # a. capabilities endpoint (probes real SDK)
  curl -sf -H "x-auth-token: $AUTH_TOKEN" http://127.0.0.1:3493/api/fresh-agent/model-capabilities/freshclaude | tee /tmp/sdk03-caps.json
  #    PASS = body has ok:true AND non-empty models[]; ok:false (typed 503 probe failure) = STOP evidence
  # b. create freshclaude pane (real session + init handshake through the bridge)
  CREATE=$(curl -sf -X POST -H "x-auth-token: $AUTH_TOKEN" -H 'content-type: application/json' \
    -d '{"agent":"claude","name":"sdk03-smoke","cwd":"/tmp/opencode/sdk03-smoke-cwd"}' \
    http://127.0.0.1:3493/api/tabs) && echo "$CREATE"
  PANE=$(node -e 'process.stdout.write(JSON.parse(process.argv[1]).data.paneId)' "$CREATE")
  # c. cheap prompt; blocks until idle; PASS = body status:'ok'. status:'approx' = deadline miss =
  #    NOT proof of turn completion — treat as failure evidence, inspect capture + server log.
  curl -sf -X POST -H "x-auth-token: $AUTH_TOKEN" -H 'content-type: application/json' \
    -d '{"text":"Reply with the single word PONG.","timeout":120}' \
    http://127.0.0.1:3493/api/panes/$PANE/send-keys | tee /tmp/sdk03-send.json
  # d. transcript (text/plain rendered) must contain PONG; then teardown
  curl -sf -H "x-auth-token: $AUTH_TOKEN" http://127.0.0.1:3493/api/panes/$PANE/capture | tee /tmp/sdk03-capture.txt
  kill "$(cat /tmp/freshell-3493.pid)" && rm -f /tmp/freshell-3493.pid
  ```
  Bootstrap precedent: a typed bootstrap/init error from create or send-keys IS SDK evidence — record and STOP; do not paper over. OAuth in-flight `system.init` must not kill the bridge (fork design-doc warning; LB-5 verified by VAL-1: benign-routing survives 0.3 message-union growth).
- [ ] **Step 4:** Commit nothing. Update run ledger §"Runtime evidence": installed agent-sdk version, probe `parsedModelIds`, contract result, smoke PASS evidence (PONG line + send body).

### Task 3: Regression-lock tests — defensive options + benign-shape tolerance (TDD)

**Files:** `test/unit/server/sdk-bridge.test.ts` (append two describes; do NOT touch existing tests)

**Real seam (LB-6d; follow the file's existing convention at L27-59):** message routing is `private handleSdkMessage(sessionId, msg)`; tests feed raw SDK messages through the existing `mockMessages` async-generator harness into `createSession`, then listen via session `subscribe`/message events. Assertions observe what REACHES listeners (user/assistant/result broadcasts) and the debug log — there are no `onUserMessage/onAssistantMessage/onResult` spies and no `rate_limit_event`/`elicitation/create` precedents to copy.

**Fork correspondence:** fork addenda at its `sdk-bridge.test.ts:1496` (options lock) and `:372` (benign shapes). Deviation: assertions pin CURRENT main semantics exactly (strip set = precisely `CLAUDECODE`, `ANTHROPIC_API_KEY`).

- [ ] **Step 1 (RED a):** `describe('createClaudeSdkOptions: 0.3 defensive-options lock')`: (i) `createClaudeSdkCleanEnv` output lacks `CLAUDECODE` and `ANTHROPIC_API_KEY` but passes through unrelated vars; (ii) `createClaudeSdkOptions({abortController})` has `settingSources` deep-equal `['user','project','local']` and carries the abortController. RED-witness: first assert `['user','project']` (deliberately wrong) → paste failing output → correct. Targeted run (explicit `--config` — without it the coordinator prepends `run --config <server>` and the bare `run` token degrades into a filename filter matching every path containing "run"; `coordinator-command-matrix.ts` `buildVitestPassthroughPhase`): `npm run test:vitest -- run test/unit/server/sdk-bridge.test.ts -t 'defensive-options lock' --config config/vitest/vitest.server.config.ts`. Unexpected PASS on the wrong assertion = invalid test, rewrite.
- [ ] **Step 2 (GREEN a):** same command → PASS, paste.
- [ ] **Step 3 (RED b):** `describe('ClaudeSdkBridge 0.3 benign control shapes')`: via the mock-generator harness, feed (i) `{type:'mcp_servers', servers:[{status:'pending'}]}`, (ii) `{type:'system', subtype:'model_refusal_fallback'}`, (iii) `{type:'system', subtype:'model_refusal_no_fallback'}`. Assertions, uniform for all three (Fresh Eyes round-2 disposition: no logger-mock machinery — the file has no logger spy convention and `server/logger.js` is captured at import time): assert no user/assistant/result listener emissions from each benign shape, THEN feed a follow-up normal assistant message and assert its broadcast arrives (positive liveness evidence the benign message was consumed without wedging the session). Verified routing (read at planning): `mcp_servers` lands in the default debug-log arm; the two `system` subtypes fall through the `system` case silently — both behaviors purely internal, hence no observation-channel assertions. RED-witness (temporarily assert a broadcast happened), then correct.
- [ ] **Step 4 (GREEN b):** full file run → all existing + new tests PASS.
- [ ] **Step 5 (REFACTOR):** factor shared harness setup only where duplication is real, matching the file's existing style. Rerun.
- [ ] **Step 6 (impacted suites):** `npm run test:vitest -- run test/unit/server/ --config config/vitest/vitest.server.config.ts`.
- [ ] **Step 7:** Commit: `test: lock claude-sdk 0.3 defensive options and benign control shapes`.

### Task 4: Expose probed claude catalog (with aliases) in the settings selector (TDD)

**Files:** `src/lib/fresh-agent-model-capabilities.ts`, `src/components/fresh-agent/FreshAgentSettingsButton.tsx`, `test/unit/client/lib/fresh-agent-model-capabilities.test.ts`, `test/unit/client/components/fresh-agent/FreshAgentSettingsButton.test.tsx`

**Why:** the probed 0.3 catalog carries alias rows and dated full IDs (VAL-1 strings evidence; live shape confirmed at Task 2 Step 1) plus `claude-opus-4-6`-era entries; static labels (`FRESH_AGENT_MODEL_OPTIONS_BY_SESSION_TYPE` = `claude-opus-4-6` alone) never match probed `id`s, so the selector stays alias-blind forever. We pre-compute merged options so the radio list's `onChange` (FreshAgentSettingsButton L239) round-trips values it rendered. NO loading gate: none exists (LB-7) — statics render instantly and merged rows swap in when the fetch resolves, matching the opencode path's existing behavior and the anti-ghost-row goal.

**Data contract (LB-8):** client capability entries are strict-schema `{id, displayName, provider, supportsEffort, supportedEffortLevels, supportsAdaptiveThinking}`. The fetch helper returns the union `FreshAgentModelCapabilitiesResponse` (ok / error); the component narrows `ok === true` (records `status:'fresh'|'cached'`) before merging — keep that narrowing at the call site so the helper takes the narrowed catalog. Dedupe key = `id` (never `value`).

**Interface (exact):**
```ts
// src/lib/fresh-agent-model-capabilities.ts (new export)
export interface ClaudeSelectorOptions {
  modelOptions: readonly FreshAgentModelOption[] // statics first, then deduped probed rows
  // (labelById was dropped during execution — a5db5aab4: nothing consumed it; probed labels live inline on each option)
}
export function mergeClaudeSelectorOptions(
  catalog: FreshAgentModelCapabilities | null | undefined,
  staticOptions: readonly FreshAgentModelOption[],
): ClaudeSelectorOptions
```
Rules: skip probed rows whose `id` equals an existing static option's value; probed row label = the row's own `displayName` (the strict-schema catalog ALREADY carries server-formatted display names — do NOT re-derive labels client-side; the server's `formatCapabilityDisplayName` in `model-capability-registry.ts` is private and unimportable from client code); `modelOptions.find(...)?.label` serves all label lookups; null/empty catalog → statics unchanged. Probed rows also carry their catalog `thinkingEfforts`, and switch-time stamping of `modelEffortLevels` onto pane content keeps the Thinking select value + launch payload consistent for probed-only models (added during execution after delta-review findings — c0dcd3dd3, 8b2abad59, 8d2cd81da).

**Component edits:**
1. Rename state slot `opencodeCapabilities` → `probedCapabilities` (all references; opencode render path otherwise byte-identical).
2. Fetch effect: extend gating from freshopencode-only to "freshopencode OR `paneContent.provider === 'claude'`" (reuse the L89 family expression's precedent — no new magic strings). Fetch by `sessionType` unconditionally — Fresh Eyes round-1 verified the capabilities route already serves kilroy via the kilroy→claude descriptor in `shared/fresh-agent.ts`.
3. `modelOptions` for claude providers = `mergeClaudeSelectorOptions(narrowed probed catalog, FRESH_AGENT_MODEL_OPTIONS_BY_SESSION_TYPE[sessionType] ?? []).modelOptions` (real exported name, already imported at L8/used at L83 — `FRESH_AGENT_MODELS` does not exist); opencode path unchanged.

- [ ] **Step 1 (RED, helper):** add tests in the lib test file: (i) catalog (STRICT-shape fixtures, cf. `CATALOG_RESPONSE`) with alias `opus` + full ID `claude-opus-4-6-20260301`-style + a row equal to the static `claude-opus-4-6` → merged = statics + the two non-duplicate probed rows, dedupe by `id`; (ii) null catalog → statics + static labels only; (iii) `models: []` → same as (ii). Run → RED (not a function), paste.
- [ ] **Step 2 (GREEN, helper):** implement; lib test file PASS.
- [ ] **Step 3 (RED, component):** in `FreshAgentSettingsButton.test.tsx` (mock the api module per the file's existing convention): freshclaude pane → popover → resolve strict-shape catalog `{status:'fresh', models:[{id:'opus',…},{id:'claude-opus-4-6-20260301',…}]}` → assert (a) fetch called once with this session's type, (b) radio options include both probed ids, (c) one row per unique id, (d) kilroy-content case also fires the fetch. Run → RED (no fetch for claude), paste.
- [ ] **Step 4 (GREEN, component):** apply the component edits; full component file PASS; existing opencode cases unchanged (regression witness).
- [ ] **Step 5 (REFACTOR):** consolidate claude/opencode branches ONLY if duplication is real; otherwise keep opencode byte-identical. Rerun both test files.
- [ ] **Step 6 (impacted suites):** `npm run test:vitest -- run test/unit/client/lib/fresh-agent-model-capabilities.test.ts test/unit/client/components/fresh-agent/FreshAgentSettingsButton.test.tsx` plus any fresh-agent client suites that render the popover (discover via the lib's import graph).
- [ ] **Step 6b (e2e gate, unconditional — Fresh Eyes round-2 finding):** `npm run test:e2e:local -- test/e2e-browser/specs/fresh-agent.spec.ts` (this spec exercises the freshclaude settings popover the task changes, e.g. checked-radio assertions at L183-213; name-scoped assertions should survive row-swaps from the live probe, but a failure caused by Task 4 must be fixed, not the spec weakened). This gate exists here because Task 5's e2e gate is conditional and may never fire.
- [ ] **Step 7:** Commit: `feat(fresh-agent): expose probed claude model catalog (incl. aliases) in settings selector`.

### Task 5: Evidence-adjudicated default-model bump (DECISION MADE at execution — this section supersedes the original conditional oracle)

**Adjudication (all receipts in task-002-report.md / task-002b-report.md / /tmp/sdk03-probe.json):**
- Probe succeeded. Live 0.3.235 `supportedModels()` values: `default` (→claude-opus-5[1m]), `opus[1m]` (→claude-opus-5[1m]), `claude-fable-5[1m]`, `sonnet` (→claude-sonnet-5), `haiku` (→claude-haiku-4-5-20251001). `claude-opus-4-6` appears NOWHERE — neither as a value nor as any entry's resolvedModel.
- Gate-3 default-pane smoke: a freshclaude pane carrying the current default `claude-opus-4-6` successfully completed a turn BUT ran as **claude-sonnet-5** — CLI 2.1.235 silently substitutes for the unrecognized model id. The old default is NOT honored under 0.3 (worse than an error: silent model drift).
- Gate-3 alias-pane smoke: `model: 'opus[1m]'` runs as **claude-opus-5**, full lifecycle OK through the bridge.
- **DECISION: new default = `opus[1m]`** (executed end-to-end evidence; alias-domain catalog means the original "highest claude-opus-* dated ID" rule and the "alias-only ⇒ skip" rule were both written against a catalog shape that turned out not to exist; not firing would leave a silently-drifting default, violating the upgrade's purpose). The original "probe failed ⇒ skip" disposition is retained for the record but not triggered.

**Files:**
- Modify: `shared/fresh-agent-models.ts` — freshclaude + kilroy single entries: value `'opus[1m]'`, label `'Claude Opus 5 (1M context)'`, thinkingEfforts `['low','medium','high','xhigh','max']` (live catalog's supportedEffortLevels). FRESHCODEX/FRESHOPENCODE entries untouched.
- Modify: `src/lib/fresh-agent-registry.ts` L67 + L104: `defaultModel: 'opus[1m]'`.
- Modify: every test expectation pinning `claude-opus-4-6` as the DEFAULT model or STATIC option list — DISCOVER by running suites and inspecting failures; grep-guard with `grep -rn "claude-opus-4-6" test/ src/ shared/ server/`. Known-but-verify pin candidates: `test/unit/server/sdk-bridge.test.ts`, `ws-handler-fresh-agent-lifecycle-parity.test.ts`, `config-store.test.ts`, `session-history-loader.test.ts`, `fresh-agent/claude-adapter.test.ts`, client store/settings suites, `test/unit/shared/fresh-agent-registry.test.ts`, `test/integration/server/settings-api.test.ts`, and the two Task-4-touched files if their fixtures reused the static. DO NOT touch: `shared/format-model-name.ts` + its test (generic pattern converter; `claude-opus-4-6` is a pattern fixture that stays green), e2e corpus/fixture data that merely names the model (e.g. `restore-matrix.spec.ts` dated-variant fixtures, session-corpus helpers, harness seed data).
- Modify: `test/e2e-browser/specs/fresh-agent.spec.ts` — the checked-radio "Claude Opus 4.6" name assertion (~L194) is a DEFAULT expectation, not fixture data; update ONLY the name string to the new label. This is a faithful expectation update, not a weakening (structure/assertion count unchanged).

- [ ] **Step 1:** RED: update the default-model expectations (changed test files + the e2e name string) against the CURRENT source → run each changed unit/integration file → paste the failures showing old default residue.
- [ ] **Step 2 (GREEN):** apply the two source edits (models table + registry defaults); rerun ALL files changed in Step 1 → PASS. Then run the full client unit suite to catch pins you missed: `npm run test:vitest -- run test/unit/ test/integration/server/settings-api.test.ts` (default config) and `npm run test:vitest -- run test/unit/server/ --config config/vitest/vitest.server.config.ts`. Any further default-pin failures = same discipline (update expectation only if it pins the default; fixtures stay).
- [ ] **Step 3 (e2e gate):** `npm run test:e2e:local -- test/e2e-browser/specs/fresh-agent.spec.ts test/e2e-browser/specs/restore-matrix.spec.ts` → PASS. Screenshot baselines (`screenshot-baselines.spec.ts`) are out of scope — if a baseline actually captures a changed default label, record and report to root; do NOT regenerate silently.
- [ ] **Step 4:** Commit: `fix(fresh-agent): bump default claude model to opus[1m] (0.3 SDK catalog evidence)`.

### Task 6: Close-out verification gate

- [ ] **Step 1:** `npm run typecheck && npm run lint` — PASS.
- [ ] **Step 2:** `FRESHELL_TEST_SUMMARY="sdk 0.3 upgrade close-out" npm test` — PASS; record `npm run test:status` holder/result pointer.
- [ ] **Step 3:** Re-run Task 2 Step 2 (real-CLI contract) once against final tree — receipts updated.
- [ ] **Step 4:** Update run-state.md to `stage: execution-complete`; STOP for root-run Fresh Eyes delta rounds. Do NOT open a PR.

## 8. Rollback

Everything is a worktree branch; rollback = abandon. Targeted, cheapest-first:
1. Selector wiring (Task 4) → revert its commit; static-only selector returns.
2. Default-model bump (Task 5) → revert its commit.
3. Whole upgrade suspect → revert Task 1 commit, `npm install` restores the old closure (Task 3's tests remain valid on 0.2.71 — the defenses they lock exist there already).
The live 3001 Rust server is untouched at every stage; no prod rollback exists because no deploy occurs.

## Appendix A: 0.3-drift contingency (executed evidence says unlikely; kept as guard)

VAL-1's tsc probe compiled main's full SDK usage surface clean against both 0.3.195 and 0.3.235 published types (only compatible deltas: `interrupt()` return widened; `CanUseTool` widened `| null` + required `requestId` context). If Task 1 Step 4/5 still fails in-tree: (1) capture exact TS errors; (2) adaptations confined to the modules that import the SDK directly (verified): `server/sdk-bridge.ts`, `server/sdk-bridge-types.ts` (SDK type re-exports), `server/fresh-agent/model-capability-registry.ts`, and the probe module — each as its own TDD micro-commit (`adapters/claude/adapter.ts` consumes the bridge, imports no SDK symbols, and changes only if a bridge signature moves); (3) extend Task 3's locks before adapting behavior. Do NOT broaden into sidecar-vendored SDK, slash-command advertising, or attachment features (ratified out of scope).
