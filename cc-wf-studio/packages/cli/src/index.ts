/**
 * `@cc-wf-studio/cli` public library API.
 *
 * The CLI binary lives in `./cli.ts`; this module re-exports the small,
 * side-effect-free helpers that downstream tooling (CI scripts, IDE
 * integrations, custom runners) may want to embed.
 */

export {};
