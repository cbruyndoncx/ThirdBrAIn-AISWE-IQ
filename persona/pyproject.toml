[project]
name = "persona"
version = "0.0.0"
description = "A python library for storing and managing LLM roles and skills."
authors = [
  {name="Jasper Ginn", email="jasperginn@gmail.com"}
]
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
    "fsspec>=2025.10.0",
    "httpx>=0.28.1",
    "orjson>=3.11.4",
    "pydantic>=2.12.3",
    "pydantic-settings>=2.11.0",
    "python-frontmatter>=1.1.0",
    "pyyaml>=6.0.3",
    "rich>=14.2.0",
    "typer>=0.20.0",
    "python-box>=7.3.2",
    "platformdirs>=4.5.0",
    "onnxruntime>=1.23.2",
    "tokenizers>=0.22.1",
    "pyarrow>=22.0.0",
    "duckdb>=1.4.3",
    "textual>=1.0.0",
]

[project.optional-dependencies]
mcp = [
    "aiofiles>=25.1.0",
    "fastmcp>=2.14.0",
    "httpx>=0.28.1",
]

[project.scripts]
persona = "persona.cli:entrypoint"

[tool.ruff]
lint.ignore = ["E501"]
extend-exclude = [
  "__pycache__",
  ".eggs",
  ".git",
  ".venv",
  "build",
  "dist",
  "notebooks",
  ".cache"
]
line-length = 100
indent-width = 4

[tool.ruff.format]
quote-style = "single"
indent-style = "space"

[tool.mypy]
plugins = ['pydantic.mypy']
follow_imports = "silent"
explicit_package_bases = true

[tool.pytest.ini_options]
markers = [
  "integration: marks integration tests",
  "llm: marks tests that require LLM calls",
]
cache_dir = "/home/vscode/workspace/.cache/pytest"

[dependency-groups]
dev = [
    "nest-asyncio>=1.6.0",
    "prek>=0.2.25",
    "ipykernel>=6.29.5",
    "pytest>=8.4.2",
    "pytest-asyncio>=1.2.0",
    "pytest-httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/persona"]
