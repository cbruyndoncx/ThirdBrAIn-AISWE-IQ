name: Verify production search

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  production-search:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify deployed search contract
        run: node scripts/check-production-search.mjs
