summary = "Dangerous mode is not normalized for Goose yet."
command = "sh"
args = ["-c", "echo danger yolo mode is not configured for goose; exit 1"]
support = "disabled"
evidence = "deterministic"
effect = "dangerous"
network = true
interaction = "noninteractive"
platforms = ["linux-x64-gnu"]
executable = "sh"
source = "internal:phase-01@fab5848"
verified_at = "2026-08-05T00:00:00Z"
